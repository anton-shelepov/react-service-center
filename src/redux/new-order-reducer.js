
let initialState = { 
    ordersImages: {
        
    }
}

const newOrderReducer = (state = initialState, action) => {
    switch (action.type) { 
        default:
            return state;
    }
}

export default newOrderReducer;